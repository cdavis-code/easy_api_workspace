// AUTO-GENERATED by easy_api_generator — DO NOT EDIT

import 'dart:async';
import 'dart:convert';

import 'package:shelf_plus/shelf_plus.dart';

import 'package:mcp_example/src/user_store.dart' as user_store;
import 'package:mcp_example/src/todo_store.dart' as todo_store;

void main() => shelfRun(init, defaultBindPort: 8080, defaultBindAddress: '0.0.0.0');

Handler init() {
  var app = Router().plus;

  app.get('/users', _handle_get_listUsers);
  app.post('/users', _handle_post_createUser);
  app.get('/users/search', _handle_get_searchUsers);
  app.get('/todos', _handle_get_listTodos);
  app.post('/todos', _handle_post_createTodo);
  app.get('/users/<usersId>', _handle_get_getUser);
  app.delete('/users/<usersId>', _handle_delete_deleteUser);
  app.get('/user_todos/<user_todosId>', _handle_get_getUser_todo);
  app.get('/todos/<todosId>', _handle_get_getTodo);
  app.delete('/todos/<todosId>', _handle_delete_deleteTodo);
  app.delete('/remove_todo_from_users/<remove_todo_from_usersId>', _handle_delete_deleteRemove_todo_from_user);
  app.get('/todos_for_users/<todos_for_usersId>', _handle_get_getTodos_for_user);

  return app.call;
}

Future<Response> _handle_get_listUsers(Request request) async {
  try {

      final result = await user_store.UserStore.listUsers();
      final serialized = _serializeResult(result);
      return Response(200,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_post_createUser(Request request) async {
  try {
    final body = await request.body.asJson as Map<String, dynamic>;
    final name = body['name'] as String;
    final email = body['email'] as String;
      final result = await user_store.UserStore.createUser(name: name, email: email);
      final serialized = _serializeResult(result);
      return Response(201,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_get_searchUsers(Request request) async {
  try {
    final queryRaw = request.url.queryParameters['q'];
    if (queryRaw == null) {
      return Response(400,
        body: jsonEncode({'error': "Missing required query parameter 'q'"}),
        headers: {'Content-Type': 'application/json'},
      );
    }
    final query = queryRaw;
      final result = await user_store.UserStore.searchUsers(query);
      final serialized = _serializeResult(result);
      return Response(200,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_get_getUser(Request request, String usersIdParam) async {
  try {
    final id = int.parse(usersIdParam);
      final result = await user_store.UserStore.getUser(id);
      final serialized = _serializeResult(result);
      return Response(200,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_delete_deleteUser(Request request, String usersIdParam) async {
  try {
    final id = int.parse(usersIdParam);
      final result = await user_store.UserStore.deleteUser(id);
      if (!result) {
        return Response.notFound(
          jsonEncode({'error': 'Resource not found'}),
          headers: {'Content-Type': 'application/json'},
        );
      }
      return Response(204);
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_get_getUser_todo(Request request, String user_todosIdParam) async {
  try {
    final userIdRaw = request.url.queryParameters['userId'];
    if (userIdRaw == null) {
      return Response(400,
        body: jsonEncode({'error': "Missing required query parameter 'userId'"}),
        headers: {'Content-Type': 'application/json'},
      );
    }
    final userId = int.parse(userIdRaw);
      final result = await user_store.UserStore.getUserTodos(userId);
      final serialized = _serializeResult(result);
      return Response(200,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_get_listTodos(Request request) async {
  try {

      final result = await todo_store.TodoStore.listTodos();
      final serialized = _serializeResult(result);
      return Response(200,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_post_createTodo(Request request) async {
  try {
    final body = await request.body.asJson as Map<String, dynamic>;
    final title = body['title'] as String;
      final result = await todo_store.TodoStore.createTodo(title: title);
      final serialized = _serializeResult(result);
      return Response(201,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_get_getTodo(Request request, String todosIdParam) async {
  try {
    final id = int.parse(todosIdParam);
      final result = await todo_store.TodoStore.getTodo(id);
      final serialized = _serializeResult(result);
      return Response(200,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_delete_deleteTodo(Request request, String todosIdParam) async {
  try {
    final id = int.parse(todosIdParam);
      final result = await todo_store.TodoStore.deleteTodo(id);
      if (!result) {
        return Response.notFound(
          jsonEncode({'error': 'Resource not found'}),
          headers: {'Content-Type': 'application/json'},
        );
      }
      return Response(204);
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_delete_deleteRemove_todo_from_user(Request request, String remove_todo_from_usersIdParam) async {
  try {
    final todoIdRaw = request.url.queryParameters['todoId'];
    if (todoIdRaw == null) {
      return Response(400,
        body: jsonEncode({'error': "Missing required query parameter 'todoId'"}),
        headers: {'Content-Type': 'application/json'},
      );
    }
    final todoId = int.parse(todoIdRaw);
    final userIdRaw = request.url.queryParameters['userId'];
    if (userIdRaw == null) {
      return Response(400,
        body: jsonEncode({'error': "Missing required query parameter 'userId'"}),
        headers: {'Content-Type': 'application/json'},
      );
    }
    final userId = int.parse(userIdRaw);
      final result = await todo_store.TodoStore.removeTodoFromUser(todoId: todoId, userId: userId);
      if (result == null) {
        return Response.notFound(
          jsonEncode({'error': 'Resource not found'}),
          headers: {'Content-Type': 'application/json'},
        );
      }
      return Response(204);
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

Future<Response> _handle_get_getTodos_for_user(Request request, String todos_for_usersIdParam) async {
  try {
    final userIdRaw = request.url.queryParameters['userId'];
    if (userIdRaw == null) {
      return Response(400,
        body: jsonEncode({'error': "Missing required query parameter 'userId'"}),
        headers: {'Content-Type': 'application/json'},
      );
    }
    final userId = int.parse(userIdRaw);
      final result = await todo_store.TodoStore.getTodosForUser(userId);
      final serialized = _serializeResult(result);
      return Response(200,
        body: jsonEncode(serialized),
        headers: {'Content-Type': 'application/json'},
      );
  } catch (e) {
    return Response.internalServerError(
      body: jsonEncode({'error': 'An error occurred while processing the request'}),
      headers: {'Content-Type': 'application/json'},
    );
  }
}

dynamic _serializeResult(dynamic result) {
  if (result == null) return null;
  try {
    if (result is Map) return result;
    if (result is List) {
      return result.map((e) {
        if (e == null) return null;
        if (e is Map) return e;
        try {
          final toJson = e.toJson;
          if (toJson != null && toJson is Function) return toJson();
        } catch (_) {}
        return e.toString();
      }).toList();
    }
    final toJson = result.toJson;
    if (toJson != null && toJson is Function) return toJson();
    return result.toString();
  } catch (_) {
    return result.toString();
  }
}
