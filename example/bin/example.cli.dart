// Generated CLI app
// DO NOT EDIT - automatically generated by mcp_generator

import 'dart:async';
import 'dart:convert';
import 'dart:io' as io;

import 'package:args/args.dart';
import 'package:args/command_runner.dart';

import 'package:mcp_example/src/user_store.dart' as user_store;
import 'package:mcp_example/src/todo_store.dart' as todo_store;

const bool _logErrors = true;

Future<void> main(List<String> argv) async {
  final runner = CommandRunner<int>(
    'example',
    'Generated CLI for annotated easy_api tools.',
  );
  runner.argParser.addFlag(
    'compact',
    negatable: false,
    help:
        'Emit results as compact (single-line) JSON instead of pretty-printed.',
  );
  runner.addCommand(_UserStoreGroupCommand());
  runner.addCommand(_TodoStoreGroupCommand());

  try {
    final code = await runner.run(argv) ?? 0;
    io.exit(code);
  } on UsageException catch (e) {
    io.stderr.writeln(e);
    io.exit(64);
  }
}

/// Serializes [result] for stdout. Defaults to pretty-printed JSON; with
/// the global `--compact` flag, emits single-line JSON.
void _emitResult(Object? result, ArgResults? globalResults) {
  if (result == null) return;
  Object? toEmit;
  try {
    if (result is num || result is bool || result is String) {
      toEmit = result;
    } else if (result is Map) {
      toEmit = result;
    } else if (result is List) {
      toEmit = result.map(_toJsonish).toList();
    } else {
      toEmit = _toJsonish(result);
    }
  } catch (_) {
    toEmit = result.toString();
  }
  final compact = globalResults != null && globalResults['compact'] == true;
  final encoded = compact
      ? jsonEncode(toEmit)
      : const JsonEncoder.withIndent('  ').convert(toEmit);
  io.stdout.writeln(encoded);
}

Object? _toJsonish(Object? e) {
  if (e == null || e is num || e is bool || e is String || e is Map) return e;
  if (e is List) return e.map(_toJsonish).toList();
  try {
    final tj = (e as dynamic).toJson;
    if (tj != null && tj is Function) return tj();
  } catch (_) {}
  return e.toString();
}

/// Reports a CLI usage / validation error and returns exit code 64.
int _usageError(String message) {
  io.stderr.writeln(message);
  return 64;
}

/// Reports an internal error and returns exit code 1.
int _internalError(Object e, StackTrace st, String name) {
  if (_logErrors) {
    io.stderr.writeln('[easy_api] $name: $e');
    io.stderr.writeln(st);
  }
  io.stderr.writeln('An error occurred while processing the request.');
  return 1;
}

class _UserStoreGroupCommand extends Command<int> {
  @override
  String get name => 'user-store';
  @override
  String get description => 'Commands for UserStore.';

  _UserStoreGroupCommand() {
    addSubcommand(_UserStoreCreateUserCommand());
    addSubcommand(_UserStoreGetUserTodosCommand());
    addSubcommand(_UserStoreGetUserCommand());
    addSubcommand(_UserStoreListUsersCommand());
    addSubcommand(_UserStoreDeleteUserCommand());
    addSubcommand(_UserStoreSearchUsersCommand());
  }
}

class _TodoStoreGroupCommand extends Command<int> {
  @override
  String get name => 'todo-store';
  @override
  String get description => 'Commands for TodoStore.';

  _TodoStoreGroupCommand() {
    addSubcommand(_TodoStoreCreateTodoCommand());
    addSubcommand(_TodoStoreGetTodoCommand());
    addSubcommand(_TodoStoreListTodosCommand());
    addSubcommand(_TodoStoreDeleteTodoCommand());
    addSubcommand(_TodoStoreCompleteTodoCommand());
    addSubcommand(_TodoStoreAssignTodoToUserCommand());
    addSubcommand(_TodoStoreRemoveTodoFromUserCommand());
    addSubcommand(_TodoStoreGetTodosForUserCommand());
  }
}

class _UserStoreCreateUserCommand extends Command<int> {
  @override
  String get name => 'create-user';
  @override
  String get description => 'Create a new user';

  _UserStoreCreateUserCommand() {
    argParser
      ..addOption(
        'name',
        mandatory: true,
        help: 'The user\'s full name (1-100 characters)',
      )
      ..addOption(
        'email',
        mandatory: true,
        help: 'A valid email address for the user',
      );
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final name = r['name'] as String;
      final email = r['email'] as String;
      if (name.length > 100) {
        return _usageError(
          'Parameter name exceeds maximum length of 100 characters.',
        );
      }
      if (email.length > 254) {
        return _usageError(
          'Parameter email exceeds maximum length of 254 characters.',
        );
      }
      if (!RegExp(r'^[\w\.-]+@[\w\.-]+\.\w+$').hasMatch(email)) {
        return _usageError('Parameter email does not match required pattern.');
      }
      final result = await user_store.UserStore.createUser(
        name: name,
        email: email,
      );
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'create-user');
    }
  }
}

class _UserStoreGetUserTodosCommand extends Command<int> {
  @override
  String get name => 'get-user-todos';
  @override
  String get description => 'Get all todos assigned to a user';

  _UserStoreGetUserTodosCommand() {
    argParser..addOption('user-id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final userId = int.parse((r['user-id'] as String?)!);

      final result = await user_store.UserStore.getUserTodos(userId);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'get-user-todos');
    }
  }
}

class _UserStoreGetUserCommand extends Command<int> {
  @override
  String get name => 'get-user';
  @override
  String get description => 'Get user by ID';

  _UserStoreGetUserCommand() {
    argParser..addOption('id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final id = int.parse((r['id'] as String?)!);

      final result = await user_store.UserStore.getUser(id);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'get-user');
    }
  }
}

class _UserStoreListUsersCommand extends Command<int> {
  @override
  String get name => 'list-users';
  @override
  String get description => 'List all users';

  _UserStoreListUsersCommand() {}

  @override
  Future<int> run() async {
    try {
      // ignore: unused_local_variable
      final r = argResults!;

      final result = await user_store.UserStore.listUsers();
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'list-users');
    }
  }
}

class _UserStoreDeleteUserCommand extends Command<int> {
  @override
  String get name => 'delete-user';
  @override
  String get description => 'Delete a user';

  _UserStoreDeleteUserCommand() {
    argParser..addOption('id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final id = int.parse((r['id'] as String?)!);

      final result = await user_store.UserStore.deleteUser(id);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'delete-user');
    }
  }
}

class _UserStoreSearchUsersCommand extends Command<int> {
  @override
  String get name => 'search-users';
  @override
  String get description => 'Search users by query';

  _UserStoreSearchUsersCommand() {
    argParser..addOption(
      'q',
      mandatory: true,
      help: 'Text to search for in user names and emails (max 1000 characters)',
    );
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final query = r['q'] as String;
      if (query.length > 1000) {
        return _usageError(
          'Parameter query exceeds maximum length of 1000 characters.',
        );
      }
      final result = await user_store.UserStore.searchUsers(query);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'search-users');
    }
  }
}

class _TodoStoreCreateTodoCommand extends Command<int> {
  @override
  String get name => 'create-todo';
  @override
  String get description => 'Create a new todo';

  _TodoStoreCreateTodoCommand() {
    argParser..addOption('title', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final title = r['title'] as String;

      final result = await todo_store.TodoStore.createTodo(title: title);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'create-todo');
    }
  }
}

class _TodoStoreGetTodoCommand extends Command<int> {
  @override
  String get name => 'get-todo';
  @override
  String get description => 'Get todo by ID';

  _TodoStoreGetTodoCommand() {
    argParser..addOption('id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final id = int.parse((r['id'] as String?)!);

      final result = await todo_store.TodoStore.getTodo(id);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'get-todo');
    }
  }
}

class _TodoStoreListTodosCommand extends Command<int> {
  @override
  String get name => 'list-todos';
  @override
  String get description => 'List all todos';

  _TodoStoreListTodosCommand() {}

  @override
  Future<int> run() async {
    try {
      // ignore: unused_local_variable
      final r = argResults!;

      final result = await todo_store.TodoStore.listTodos();
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'list-todos');
    }
  }
}

class _TodoStoreDeleteTodoCommand extends Command<int> {
  @override
  String get name => 'delete-todo';
  @override
  String get description => 'Delete a todo';

  _TodoStoreDeleteTodoCommand() {
    argParser..addOption('id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final id = int.parse((r['id'] as String?)!);

      final result = await todo_store.TodoStore.deleteTodo(id);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'delete-todo');
    }
  }
}

class _TodoStoreCompleteTodoCommand extends Command<int> {
  @override
  String get name => 'complete-todo';
  @override
  String get description => 'Mark a todo as completed';

  _TodoStoreCompleteTodoCommand() {
    argParser..addOption('id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final id = int.parse((r['id'] as String?)!);

      final result = await todo_store.TodoStore.completeTodo(id);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'complete-todo');
    }
  }
}

class _TodoStoreAssignTodoToUserCommand extends Command<int> {
  @override
  String get name => 'assign-todo-to-user';
  @override
  String get description => 'Assign a todo to a user';

  _TodoStoreAssignTodoToUserCommand() {
    argParser
      ..addOption('todo-id', mandatory: true, help: '')
      ..addOption('user-id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final todoId = int.parse((r['todo-id'] as String?)!);
      final userId = int.parse((r['user-id'] as String?)!);

      final result = await todo_store.TodoStore.assignTodoToUser(
        todoId: todoId,
        userId: userId,
      );
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'assign-todo-to-user');
    }
  }
}

class _TodoStoreRemoveTodoFromUserCommand extends Command<int> {
  @override
  String get name => 'remove-todo-from-user';
  @override
  String get description => 'Remove a user from a todo';

  _TodoStoreRemoveTodoFromUserCommand() {
    argParser
      ..addOption('todo-id', mandatory: true, help: '')
      ..addOption('user-id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final todoId = int.parse((r['todo-id'] as String?)!);
      final userId = int.parse((r['user-id'] as String?)!);

      final result = await todo_store.TodoStore.removeTodoFromUser(
        todoId: todoId,
        userId: userId,
      );
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'remove-todo-from-user');
    }
  }
}

class _TodoStoreGetTodosForUserCommand extends Command<int> {
  @override
  String get name => 'get-todos-for-user';
  @override
  String get description => 'Get all todos assigned to a user';

  _TodoStoreGetTodosForUserCommand() {
    argParser..addOption('user-id', mandatory: true, help: '');
  }

  @override
  Future<int> run() async {
    try {
      final r = argResults!;
      final userId = int.parse((r['user-id'] as String?)!);

      final result = await todo_store.TodoStore.getTodosForUser(userId);
      _emitResult(result, globalResults);
      return 0;
    } catch (e, st) {
      return _internalError(e, st, 'get-todos-for-user');
    }
  }
}
