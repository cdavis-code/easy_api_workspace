# Example

See the [example](https://github.com/cdavis-code/easy_api_workplace/tree/main/example) directory in the workspace root for a complete working example that demonstrates usage of both `easy_api_annotations` and `easy_api_generator` packages together.
